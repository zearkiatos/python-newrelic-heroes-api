version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python-newrelic-heroes-api
    restart: always
    volumes:
      - ".:/app"
    env_file:
      - .env
    ports:
      - "5001:5000"
    tty: true